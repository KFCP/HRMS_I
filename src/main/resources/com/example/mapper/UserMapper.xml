<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.UserDAO">

    <select id="getUserByUsername" resultType="UserBean" parameterType="string">
        SELECT id, username, password FROM users WHERE username = #{username}
    </select>

    <!-- 
        The original isValidUser in UserDAOImpl fetched the password and compared it in Java.
        This implementation directly checks in SQL.
        The UserDAO interface method returns boolean.
    -->
    <select id="isValidUser" resultType="boolean">
        SELECT count(*) > 0 FROM users WHERE username = #{username} AND password = #{password}
    </select>

    <insert id="addUser" parameterType="UserBean" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password) VALUES (#{username}, #{password})
    </insert>

    <update id="updateUser" parameterType="UserBean">
        UPDATE users 
        SET username = #{username}, 
            password = #{password} 
        WHERE id = #{id}
    </update>

    <delete id="deleteUser" parameterType="int">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <select id="getAllUsers" resultType="UserBean">
        SELECT id, username FROM users ORDER BY username
        <!-- Password is intentionally omitted for security in a list view -->
    </select>

    <select id="getUserById" resultType="UserBean" parameterType="int">
        SELECT id, username, password FROM users WHERE id = #{id}
    </select>

</mapper>
